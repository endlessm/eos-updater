# This configuration file contains the default values for all configuration
# keys. To customise it, copy it to /etc/eos-updater/eos-updater.conf
# and edit it.
[Download]
Order=main;

# Uncomment this, set the path, and add ‘volume’ to the Download.Order, to
# enable updates from a USB volume.
# [Source "volume"]
# Path=/path/to/volume/mount
